The results of the model training show a clear difference between the two datasets. For the weather series, the sequential models performed best: both RNN and LSTM reached low and stable validation losses, and their prediction curves followed the true series very closely across peaks, drops, and short-term fluctuations. The Transformer was able to learn the overall shape, but its validation loss was less stable and its predictions showed a noticeable downward bias in the later segment. The MLP captured the broad trend only, with noisier validation behaviour and weaker alignment to the true signal, which suggests that flattening the window loses useful temporal structure for this dataset.

For the Apple series, after transforming the target into log returns, the behaviour of the models changes significantly. The sequence-based models (RNN, LSTM, and Transformer) now outperform the MLP, as they are able to capture short-term temporal dependencies in the return series. Among them, the LSTM and Transformer achieve the best performance, with lower error metrics and more stable validation behaviour, while the RNN also performs well but shows slightly higher variability. Their prediction curves fluctuate around the true series and are able to follow the general direction of movements, although they tend to smooth out extreme spikes due to the noisy nature of financial returns.

In contrast, the MLP performs worse in this setting. Its predictions show weaker alignment with the true series and higher error values, indicating that treating the input window as a flat vector is less effective when the task requires modelling sequential dependencies. Overall, these results demonstrate that once the non-stationary trend is removed, recurrent and attention-based models are better suited for the Apple dataset, as they can leverage temporal structure that the MLP cannot capture.

This leads naturally to the next part of the project: a formal model comparison and discussion, where you summarize which architecture worked best on each dataset and explain how the data characteristics influenced the results.