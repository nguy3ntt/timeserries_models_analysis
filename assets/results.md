The results of the model training shows a clear difference between the two datasets. For the weather series, the sequential models performed best: both RNN and LSTM reached low and stable validation losses, and their prediction curves followed the true series very closely across peaks, drops, and short-term fluctuations. The Transformer was able to learn the overall shape, but its validation loss was less stable and its predictions showed a noticeable downward bias in the later segment. The MLP captured the broad trend only, with noisier validation behaviour and weaker alignment to the true signal, which suggests that flattening the window loses useful temporal structure for this dataset.

For the Apple series, the task is clearly harder. The RNN gave the most reasonable result among the four models, since its predictions followed the upward movement and changing level better than the others. The LSTM learned the overall direction but consistently predicted below the true series, indicating underestimation of the level. In contrast, the Transformer and especially the MLP showed weak generalization: their training losses dropped quickly, but validation loss stayed much higher, and the prediction curves failed to track the real test behaviour. Overall, these results suggest that recurrent models are more suitable for the current forecasting setup, while the Apple dataset remains much more difficult because its signal is noisier and less regular than the weather data.

This leads naturally to the next part of the project: a formal model comparison and discussion, where you summarize which architecture worked best on each dataset and explain how the data characteristics influenced the results.